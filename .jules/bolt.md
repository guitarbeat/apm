# Bolt's Journal

## 2024-05-21 - [Initial Setup]
**Learning:** The environment requires strict .gitignore rules for `node_modules` to avoid file count limits during `pnpm install`.
**Action:** Ensure .gitignore includes `node_modules/`, `**/node_modules/`, and specific project paths like `upstream-apm/node_modules/`.
